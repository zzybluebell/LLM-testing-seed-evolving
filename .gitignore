# secrets / local env
.env
.venv/
.claude/

# python
__pycache__/
*.pyc
.DS_Store

# run artifacts: keep session.jsonl / check.json / telemetry.json / cost.json / meta.json / work/out,
# drop live streams, Claude Code local state, and failed attempts
runs/**/live.jsonl
runs/**/claude_config/
runs/**/*_failed_attempt*/
runs/**/stderr.log
runs/live.jsonl
runs/matrix.json
